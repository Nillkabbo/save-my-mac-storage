exclude_dirs:
  - 'tests'
  - 'test_*'
skips:
  - 'B108'  # hardcoded_tmp_directory - Expected for system cleaner
  - 'B110'  # try_except_pass - Used for graceful error handling  
  - 'B112'  # try_except_continue - Used for graceful error handling
  - 'B404'  # blacklist - subprocess module needed for system operations
  - 'B603'  # subprocess_without_shell_equals_true - Used with sanitized input
  - 'B607'  # start_process_with_partial_path - 'open' command is safe macOS system command
